import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
	palette: {
		primary: {
			dark: "#685bb5",
            main: "#7d66f2",
            light: '#afa2f5'
		},
		background: {
			default: '#e7e4f7',
		}
	},
});

export default theme;
